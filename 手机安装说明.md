# 把贪吃蛇添加到手机桌面

这是可安装的网页应用（PWA）。安装后可以从桌面图标打开；首次联网打开并显示“离线已就绪”后，可以断网玩。三种皮肤、触屏方向键、成绩记录和原有玩法均保留。

## 发布后安装

游戏网址：[打开贪吃蛇](https://andoan1999.github.io/snake-game/)。请使用这个 HTTPS 地址安装。

### 安卓

1. 用 Chrome 或支持安装网页应用的浏览器打开游戏网址。
2. 等待菜单底部显示“离线已就绪 · 断网也能玩”。
3. 展开“添加到桌面”，点击“安装贪吃蛇”；如果没有此按钮，从浏览器菜单选择“安装应用”或“添加到主屏幕”。
4. 按系统提示添加，从桌面图标打开游戏。

不同安卓浏览器可能生成独立应用或网页快捷方式，安装菜单名称也可能不同。[Google 的 PWA 安装说明](https://web.dev/learn/pwa/installation/)

### iPhone

1. 用 Safari 打开游戏网址。
2. 点“分享”；部分 Safari 布局需要先点“更多”，再点“分享”。
3. 选择“添加到主屏幕”。如果出现“作为网页 App 打开”，保持开启，再点“添加”。
4. 从桌面上的“贪吃蛇”图标打开一次，联网等待“离线已就绪”，之后可断网玩。

菜单没有“添加到主屏幕”时，可在分享菜单底部的“编辑操作”里添加。[Apple 的安装说明](https://support.apple.com/en-nz/guide/iphone/iphea86e5236/ios)

## 离线、成绩和更新

- 首次使用必须联网，准备好全部离线内容。中断时会提示重试。
- 分数、排行榜、皮肤保存在当前浏览器或当前桌面应用中，不跨设备同步。iPhone 桌面应用与原 Safari 页面可能使用各自的存储。
- 切到后台会暂停；系统彻底关闭进程后，正在进行的这一局不会自动恢复。
- 发现新版时菜单会提示。关闭所有已打开的游戏网页和应用窗口，稍后再打开即可使用新版；游戏不会中途强制刷新。
- 浏览器清理网站数据或系统回收离线内容后，需要重新联网准备；清除网站数据也可能清除成绩。

## 部署文件

把以下文件放在同一个 HTTPS 网站目录，保留 `icons` 子目录结构：

```text
index.html
pwa.js
sw.js
manifest.webmanifest
icons/
  icon.svg
  icon-192.png
  icon-512.png
  icon-maskable-512.png
  apple-touch-icon.png
```

所有路径均为相对路径，适用于 GitHub Pages 的 `/snake-game/` 子目录，也适用于网站根目录。不能只上传 `index.html`。

开发时可在本机通过 localhost 测试。双击 HTML 文件或让手机访问普通 HTTP 局域网地址，不能完成正常的 Service Worker 离线安装。

以后修改任一应用资源时，同时修改 `sw.js` 顶部的 `VERSION`，并一次发布整套资源。新版本缓存全部成功后才会启用。[Service Worker 生命周期说明](https://developer.mozilla.org/en-US/docs/Web/API/Service_Worker_API/Using_Service_Workers)

## 当前验证范围

已使用本机 Chrome 浏览器引擎完成 15 项自动检查：应用清单与安装条件、离线重开与带参数入口、触屏开始/暂停/继续/转向、成绩和皮肤留存、三种皮肤在三种手机尺寸下的安全区布局、iPhone 安装提示与模拟独立窗口、缓存失败重试、升级与缓存隔离。

手机尺寸、触屏和安全区为模拟测试。尚未在实体安卓或 iPhone 上完成桌面安装、系统重启后启动及声音体验测试。
